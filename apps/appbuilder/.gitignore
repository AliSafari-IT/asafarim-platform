# Playwright output — regenerated by `pnpm e2e`
.playwright/
test-results/

# Session cookies minted by tests/e2e/global-setup.ts — local-only, never committed
tests/e2e/.auth/

# Bundled worker build output (tsup) — regenerated by `pnpm worker:build`
worker-dist/
